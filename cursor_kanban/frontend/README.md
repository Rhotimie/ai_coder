# Kanban

Single-board Kanban app. Five renameable columns, cards with a title and details, drag and drop.

```bash
npm install
npx playwright install chromium
npm run dev
# http://127.0.0.1:3001
```

```bash
npm test
npm run test:e2e
```
